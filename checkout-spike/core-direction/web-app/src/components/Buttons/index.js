export { default as Button } from "./Button.vue";

const BUTTON_TYPES = {
  PRIMARY: "primary",
  SECONDARY: "secondary",
  GRADIENT: "gradient",
  DEFAULT: "default",
};

const BUTTON_STYLE = {
  ROUND_EDGE: "round-edge",
  ROUND: "round",
  SQUARE: "square",
};

const BUTTON_STATE = {
  ACTIVE: "active",
};

export { BUTTON_TYPES, BUTTON_STYLE, BUTTON_STATE };
